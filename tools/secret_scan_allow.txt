# Keys in a secrets file whose VALUE is not a secret, so the value scan skips
# them (tools/secret_scan.py). One exact key name per line; no globs, so a new
# key is scanned until someone decides otherwise here.
#
# Only addresses belong here: URLs, hosts, ports. A name containing TOKEN,
# PASS, SECRET or KEY is refused by the scanner even if listed.
#
# NTFY_URL: the ntfy server address. It sits in secrets.env next to the ntfy
# token, but values like https://ntfy.sh or http://127.0.0.1:2586 also appear
# (correctly) in deploy/pi/bootstrap.sh and the docs.
NTFY_URL
