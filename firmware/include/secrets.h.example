#pragma once

// Copy this file to secrets.h and put your real values in it. secrets.h is
// gitignored; this example is not.
//
// The build works without secrets.h -- config.h falls back to a default -- so a
// fresh clone compiles and runs. Change the password before using it anywhere
// you care about.

#define WIFI_AP_PASS "changeme123"
